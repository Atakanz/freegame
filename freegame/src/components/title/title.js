import { View, Text } from "react-native";
import React from "react";
import styles from "./title.style";

const Title = () => {
  return (
    <View>
      <Text style={styles.logo}>FREETOGAME</Text>
    </View>
  );
};

export default Title;
